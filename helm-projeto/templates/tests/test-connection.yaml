apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "projeto-devops.fullname" . }}-test-connection"
  labels:
    {{- include "projeto-devops.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "projeto-devops.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
